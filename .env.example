# OIDC is required for the management UI. Register this redirect URI in
# Authelia: https://your-domain.example/api/auth/callback

# [Required] Authelia issuer URL
# OIDC_ISSUER_URL=https://auth.example.com

# [Required] Authelia OIDC client ID
# OIDC_CLIENT_ID=tgstate

# [Required] Authelia OIDC client secret
# OIDC_CLIENT_SECRET=

# [Recommended before first OIDC login] Public access URL used for share links
# and the OIDC callback URL. Register BASE_URL/api/auth/callback in Authelia.
# BASE_URL=https://your-domain.example

# [Optional] Telegram Bot Token from @BotFather
# BOT_TOKEN=

# [Optional] File storage channel (@channel_name or -100xxxxxxxxxx)
# CHANNEL_NAME=

# [Optional] Log level (default: info)
# LOG_LEVEL=info

# [Optional] Data directory (default: app/data)
# DATA_DIR=app/data
